<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:toolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit"
        x:Class="PatientMonitor.MainWindow"
        mc:Ignorable="d"
        Title="Patient Monitor" Height="700" Width="800">

    <Grid Margin="10">
        <!-- Chart Control for Real-Time Data Visualization -->
        <toolkit:Chart x:Name="chartECG" Title="Real-Time Parameter" Width="700" Height="300" VerticalAlignment="Top">
            <toolkit:LineSeries x:Name="lineSeries" Title="Parameter Channel1"
                                IndependentValueBinding="{Binding Key}"
                                DependentValueBinding="{Binding Value}">
                <toolkit:LineSeries.IndependentAxis>
                    <toolkit:LinearAxis Orientation="X" Title="Time (s)" />
                </toolkit:LineSeries.IndependentAxis>
                <toolkit:LineSeries.DependentRangeAxis>
                    <toolkit:LinearAxis Orientation="Y" Title="Amplitude (mV)" />
                </toolkit:LineSeries.DependentRangeAxis>
            </toolkit:LineSeries>
        </toolkit:Chart>

        <!-- Patient Information Group -->
        <GroupBox Header="Input Patient" Width="300" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="40,320,0,0" FontSize="14">
            <StackPanel>
                <Label Content="Patient Name:" />
                <TextBox x:Name="textBoxName" Width="250" />

                <Label Content="Patient Age:" />
                <TextBox x:Name="textBoxAge" Width="250" PreviewTextInput="textBoxAge_PreviewTextInput" />

                <Label Content="Date of Study:" />
                <DatePicker x:Name="datePickerMonitor" Width="250" />

                <Button x:Name="createPatientButton" Content="Create Patient" Width="250" Click="CreatePatientButton_Click" />
                <Button x:Name="updatePatientButton" Content="Update Patient" Width="250" IsEnabled="False" />
            </StackPanel>
        </GroupBox>

        <!-- Parameters Group -->
        <GroupBox Header="Parameters" Width="300" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,320,40,0" FontSize="14">
            <StackPanel>
                <Label Content="Amplitude (mV)" Margin="5,5,5,0" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="0 mV" VerticalAlignment="Center" Margin="5,0,0,0"/>
                    <TextBlock Text="2.5 mV" VerticalAlignment="Center" Margin="15,0,0,0"/>
                    <TextBlock Text="5 mV" VerticalAlignment="Center" Margin="15,0,0,0"/>
                    <TextBlock Text="7.5 mV" VerticalAlignment="Center" Margin="15,0,0,0"/>
                    <TextBlock Text="10 mV" VerticalAlignment="Center" Margin="15,0,5,0"/>
                </StackPanel>
                
                <Slider x:Name="sliderAmplitude" Minimum="0" Maximum="10" Width="250" ValueChanged="SliderAmplitude_ValueChanged" />

                <Label Content="Frequency (cycles/min):" />
                <TextBox x:Name="textBoxFrequency" Width="250" TextChanged="textBoxFrequency_TextChanged" />

                <Label Content="Harmonics:" />
                <ComboBox x:Name="comboBoxHarmonics" Width="250" IsEnabled="False" SelectionChanged="ComboBoxHarmonics_SelectionChanged">
                    <ComboBoxItem Content="1" IsSelected="True"/> 
                    <ComboBoxItem Content="2" />
                    <ComboBoxItem Content="3" />
                </ComboBox>

                <Label Content="Parameter:" />
                <ComboBox x:Name="comboBoxParameters" Width="250" IsEnabled="False">
                    <ComboBoxItem Content="ECG" IsSelected="True"/>
                    <ComboBoxItem Content="EEG" />
                    <ComboBoxItem Content="EMG" />
                    <ComboBoxItem Content="Respiration" />
                </ComboBox>
            </StackPanel>
        </GroupBox>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,20">
            <Button x:Name="startSimulationButton" Content="Start Simulation" Width="150" Height="40" Click="StartSimulationButton_Click" IsEnabled="False"/>
            <Button x:Name="stopSimulationButton" Content="Stop" Width="150" Height="40" Click="StopSimulationButton_Click" Margin="10,0,0,0" IsEnabled="False" />
        </StackPanel>
    </Grid>
</Window>
